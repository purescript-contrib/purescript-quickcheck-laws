{
  "private": true,
  "scripts": {
    "postinstall": "bower install",
    "build": "pulp build && pulp test"
  },
  "devDependencies": {
    "bower": "^1.7.9",
    "pulp": "^9.0.1",
    "rimraf": "^2.4.1"
  }
}
